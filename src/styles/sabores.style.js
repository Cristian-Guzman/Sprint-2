import styled from "styled-components";

export const ContenedorSabor = styled.article`
    display: flex;
    flex-wrap: wrap;
    gap: 5rem;
    
    .op {
        opacity: 1;
    }
    .opno {
        opacity: .5;
    }
`

export const TituloSabor = styled.h2`
    font-size: 2.2rem;
    margin-top: 4rem;
`

export const SaboresImagen = styled.img`
    width: 7rem;
    opacity: .5;
`